var React = require('React');

var Parent = React.createClass({

    render: function() {
        return (
            <div>
              Parent component.
            </div>
        );
    }

});

module.exports = Parent;